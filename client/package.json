{
  "name": "@portfolio/client",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Freelance web developer portfolio for Ryan Chung. Statically prerendered Vite + React site.",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --ssr src/entry-server.jsx --outDir server-build && node scripts/prerender.mjs",
    "build:client": "vite build",
    "build:ssr": "vite build --ssr src/entry-server.jsx --outDir server-build",
    "prerender": "node scripts/prerender.mjs",
    "preview": "vite preview --outDir dist"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router-dom": "^7.1.5"
  },
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "@vitejs/plugin-react": "^5.0.0",
    "vite": "^7.0.0"
  }
}
