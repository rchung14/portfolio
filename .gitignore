# dependencies (root + all workspaces)
node_modules

# build output
dist
server-build

# env files — keep the example templates
.env
.env.local
.env.*.local
!.env.example

# misc
*.local
.DS_Store
.vite
npm-debug.log*
